# B2 admission: the actual low-source gap

campaign_id: one-tenth-endpoint-geometry-20260905-b1
round: b2-low-source (continuation, not a budget reset)
mode: analytic proof / exact falsification only

B1 geometry has been independently accepted at integration ae8e3c68. Its three worker lanes used6,7,12 preregistered fixed controls (25 distinct controls); normal/optimized replays and parent fixed-scalar consequences are retained in their logs, not reset. No numerical optimizer, random scout or box cover has been used. All earlier unrelated cell campaigns remain frozen.

## Exact live target

Read `../endpoint-geometry-b1/CONDITIONAL_TENTH_BRIDGE.md` in full. Its hand-chosen disjoint-bank composition gives area0.1 if the following low-source statement is proved (and the assembly itself survives independent review):

For a Borel unit-chord selector with common star center0, |h|<=1/5, and low direction set L={M<99/100},

`integral_0^{1/2} r |S_r(L)| dr >= |L|/(10*pi)`.                 (IL)

A stronger sufficient pointwise candidate is

`|S_r(V)| >= min(11/15,1-2r)|V|`, 0<r<1/2, every Borel V⊆L.   (PL)

PL is not accepted, not required if IL can be proved directly, and not a necessary condition for the original problem. A literal counterexample to PL does not reject IL. A counterexample to fixed-bank IL does not reject globalarea0.1 or rule out using global low/high coherence.

## Real geometry that the proof must retain

All chords arbitrary s(q),h(q), not centered or equal-height. Far orientation gives endpoints(x-1,h),(x,h), with x>=1/2 and M<99/100 implying x<1. M,N satisfy the exact unit geometry. For r>|h|, both present circular lobes and their opposite ideal anchors share the same height and chord. At r<=|h| the source is one merged cone, not two disjoint objects. Different labels may overlap on either physical sheet. No free symmetrization, labelwise summation, synthetic effective chord, or unproved weighted interval-union principle.

The scalar inequality `((M-r)_+)/(M+r)+((N-r)_+)/(N+r)>=1-2r` follows from M+N>=1. It is motivation ONLY, not the missing union theorem.

## Independent lanes and scope

1. `paired-pointwise/`: prove PL from actual paired geometry or give a literal positive-measure-cut counterexample. A finite list of direction points has measure zero and is not a counterexample. An abstract independent-interval example must be labelled as such until unit-chord realizability is demonstrated.
2. `integrated-low/`: target IL directly, independently of PL. Use integrated truncation/overlap, direction-measure or cross-radius geometry as appropriate. Do not merely integrate the B1 common-uniform far kernel, whose method ceiling is already below0.1. An alternate sufficient coupled low/high statement is acceptable only with its actual path to the original target spelled out.
3. Independent read-only review of the conditional bridge: all cut quantifiers, height/recovery, kernels, fixed prices, poles, countable tail and exact constants. Do not attempt to prove PL or IL while auditing an implication.

## Budget and stop rules

Theory first. Per constructive lane: at most8 NEW fixed exact controls, each individually specified before execution. Arithmetic subprocess <=60seconds, cumulative <=180seconds per lane; one arithmetic subprocess at a time. Review lane: at most120seconds total tiny arithmetic. Maximum three agents. This adds no search/grid/LP/SAT/interval/campaign authorization. All replays count as execution but do not create new control points; report both.

Each candidate's first exact counterexample stops work on that candidate; preserve it and distinguish its scope. A timeout stops that symbolic objective, not a reason to switch parameters and repeat. No broad curve fitting, family sweep, or hidden numerical tuning of a proposed counterexample.

Success: a complete universal IL/PL proof with literal source-once accounting, a precise realizable counterexample, or a genuinely new weaker sufficient lemma with an explicit unproved gap. Failure: only restating the scalar endpoint sum, renaming the missing union step, or repeating the old far-kernel ceiling. Return honest OPEN/BLOCK if that is the outcome.

Own isolated worktrees and own subtree only. Accepted baseline/B1 files are read-only. Local commits, no push. No0.25 work, no Lean builds, no old-root campaigns. Parent verifies the actual artifacts and independently reviews any new proof before changing the global bound.
