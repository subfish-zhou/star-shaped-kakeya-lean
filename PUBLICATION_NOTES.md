# 公开交付版本说明

本次用户明确要求把历史 PDF、Markdown 和完整材料 ZIP 上传到公开的 `subfish-zhou/star-shaped-kakeya-lean`。这不改变旧原件在形成时的状态。

- `history-materials` 分支保存本包可浏览原件；主分支 `history/` 保存排版 PDF、在线阅读稿、PDF 源和完整 ZIP。历史 Lean 文件不进入主分支，避免主工程的递归源码检查误收不同版本的快照。
- 原有 383 份精选原件的字节和 SHA-256 保持不变。`SOURCE_MANIFEST.json`、原生模型答卷、旧验收报告中的私人交付或未公开标记均是历史记录，不改写。
- 写作冻结稿是 `writing-evidence/r4-manuscript.md`，SHA-256 为 `0156cb9c781c4e99d9df09162315137baca0c9798ff63c8a7aaf3fd9679ecb7a`。
- 当前 `KAKEYA_HISTORY.md` 只作排版和交付元数据调整：相邻引用之间加空格，防止 Markdown 把两个引用解析成一个；字面的星号转义，防止 `m*`、`A*` 变为斜体标记；末尾私人交付说明改为本次明确授权的公开状态。没有修改数学内容、常数、历史顺序或证明证据等级。主分支在线稿另外把原件链接改为归档提交的永久链接。
- ZIP 中保持材料原目录与本地 Markdown 链接，并加入本次 PDF、TeX 源及独立哈希清单。PDF 的来源链接指向 GitHub 固定快照。
- 本次没有重新启动 Lean，没有重新运行历史数值证书，也没有重开已完成的四轮复述。模型复述不是人类同行评审；历史缺失日志仍按原审计记为缺失。
- 材料的公开保存不授予新的许可。第三方文献、依赖源码和文档保留原有权利与来源；公开访问不等于可以任意再分发或另行许可。

不同交付版本的整体清单与文件数可能不同；复核时使用当前目录的 `MANIFEST.json`，不要把早期验收记录的文件数套用到新包。
